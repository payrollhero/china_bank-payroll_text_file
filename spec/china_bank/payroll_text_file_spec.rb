require 'spec_helper'

describe ChinaBank::PayrollTextFile do
  it 'has a version number' do
    expect(ChinaBank::PayrollTextFile::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
